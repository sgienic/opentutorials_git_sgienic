<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN" "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">

<mapper namespace="kr.co.hecorea.monitor.dao.Monitor">

	<select id="select_day" resultType="hashmap">
		SELECT '22222' as a from dual
	</select>

    <select id="getOracleInfo" resultType="hashmap" parameterType="string">
		select
		 (select RTRIM(TO_CHAR(  sum(bytes)/1024/1024/1024 , 'FM999990D99'), '.') GB from dba_data_files) AS orcl,
		 (select RTRIM(TO_CHAR(   sum(bytes)/1024/1024/1024, 'FM999990D99'), '.') GB from dba_segments) AS used,
		 sysdate time
		 from dual 
	</select>
</mapper>